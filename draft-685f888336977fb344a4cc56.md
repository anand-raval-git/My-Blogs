---

---

install java jdk and Jenkins in machine

access Jenkins via port 8080 and create declrative pipeline (2nd option) give name and also give GitHub repo and save it now pipeline is ready without code

install [docker.io](http://docker.io) and docker compose in machine sudo usermod -aG docker $USER (user will add in docker group) sudo usermod -aG docker Jenkins sudo reboot

Now we have to create SonarQube servr basically there are two part 1.sonar scanner :- this is installed in Jenkins and it will scan code in GitHub repo and send to SonarQube server with compress file 2.sonarqube server :- we can create SonarQube server with docker images and this server will check bug,vulnerability,code smell etc

docker run -itd --name sonarqube -p 9000:9000 sonarqube:lts-community

Now open sonarqube server and generate token Adminstration&gt;security&gt;user&gt;token Now in Jenkins install plugin for sonarqube dashboard&gt;managae Jenkins&gt;plugins&gt;sonarqube scanner

Now time to add credential of sonarqube token and docker credential dashboard&gt;managae Jenkins&gt;credentials&gt;system&gt;global kind:- secret text

Now time to link Jenkins and sonarqube server go into Jenkins system and find sonarqube servers Now time to configure sonar scanner tool which is installed via plugin in Jenkins go into tool basically sonar is there but not enable just go into tool&gt;sonarqube installation

Let's create webhook webhook means when Jenkins code sent to sonar and sonar should give acknowledgment to Jenkins that code came that's in webhook just give url to sonarqube in sonarqube go into configration&gt;webhooks url/sonarqube-webhook/